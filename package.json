{
  "name": "@corets/store",
  "description": "Observable store that can be used inside and outside of React",
  "version": "0.1.0",
  "author": "Maxim Kott",
  "license": "MIT",
  "main": "dist/index.js",
  "module": "dist/esm/index.js",
  "types": "dist/index.d.ts",
  "keywords": [
    "corets",
    "store",
    "observable"
  ],
  "files": [
    "/dist"
  ],
  "repository": "github:corets/store",
  "homepage": "https://corets.github.io/store",
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org"
  },
  "scripts": {
    "test": "jest",
    "coverage": "jest --coverage && echo '\nOpen \"coverage/lcov-report/index.html\" in your browser\n'",
    "lint": "tsc --noEmit",
    "format": "prettier --write .",
    "build": "rm -rf dist && tsc --noEmit && tsup src/index.ts --clean --dts --sourcemap --format esm,cjs --legacy-output",
    "release": "np --no-release-draft --tag next",
    "preview": "np --no-release-draft --tag dev",
    "version": "yarn run build"
  },
  "dependencies": {
    "fast-deep-equal": "^3.1.3",
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "@types/jest": "^26.0.24",
    "@types/lodash": "^4.14.171",
    "jest": "^27.0.6",
    "np": "^7.2.0",
    "prettier": "2.2.1",
    "ts-jest": "^27.0.3",
    "tsup": "^4.12.5",
    "typescript": "^4.3.5"
  }
}
